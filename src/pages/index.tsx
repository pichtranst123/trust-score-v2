import type { NextPage } from "next";

// @component
import { Hero, Space } from "view";

//--------------------------------------------------------------------

const Home: NextPage = () => {
  return (
    <div>
      <Hero />
      <Space />
    </div>
  );
};

export default Home;
